# Cargo.toml

[package]
name = "swiftui-skia"
version = "0.0.1"

[lib]
crate-type = ["staticlib"]

[build-dependencies]
swift-bridge-build = "0.1"

[dependencies]
base64 = "0.22.1"
soft_skia = "0.10.0"
swift-bridge = "0.1"
cssparser = "0.29"